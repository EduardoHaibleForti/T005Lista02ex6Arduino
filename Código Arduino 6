int randomValues[5];  

void setup() {
  Serial.begin(9600);  

  // Preenche o vetor com valores aleatórios
  for (int i = 0; i < 5; i++) {
    randomValues[i] = random(0, 101);  
  }

  
  for (int i = 0; i < 5; i++) {
    Serial.println(randomValues[i]);
  }
}

void loop() {
  
}
